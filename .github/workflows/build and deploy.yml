name: 🛡️ Python Build and Deploy to Minikube

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build-and-deploy:
    name: Build Docker Image and Deploy to Minikube
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- Build Docker image ---
      - name: Build Docker image
        run: |
          docker build -t my-python-app:latest .
